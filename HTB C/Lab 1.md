
SimpleCTS running on port 80 

/admin 
admin:admin 

Edit theme to get php reverse shell 
upgrade shell 
run linEnum 
use /bin/sh/php upgrade 
get root flag 

echo -n "stuff" | iconv -f -UTF-8 -t UTF-16LE | base64 

import base64 
cmd = 'Write-Output "hello"' 
print(base64.b64encode(cmd.encode('utf161e')).decode() 

